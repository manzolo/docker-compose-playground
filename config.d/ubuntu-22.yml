images:
  ubuntu-22:
    category: linux
    description: Ubuntu 22.04 LTS (Jammy Jellyfish)
    image: ubuntu:22.04
    keep_alive_cmd: sleep infinity
    shell: /bin/bash
    motd: |
      ╔══════════════════════════════════════════════════════════════╗
      ║           Ubuntu 22 Quick Reference                          ║
      ╚══════════════════════════════════════════════════════════════╝

      📦 Package Manager (apt):
         apt update                               # Update package index
         apt install package_name                 # Install package
         apt install nano                         # Install package
         apt search nano                          # Search package
         apt list --installed                     # List installed packages

      🔧 System Information:
         uname -a                                 # Kernel and system info
         lsb_release -a                           # Ubuntu version
         df -h                                    # Disk usage
         free -h                                  # Memory usage

      💡 Tips:
         • Ubuntu is Debian-based
         • Use /shared for persistent files
         • Common packages: build-essential, git, curl

      📚 Documentation: https://ubuntu.com/server/docs
