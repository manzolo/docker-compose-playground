images:
  golang-1.22:
    category: programming
    description: Go 1.22 (Latest)
    image: golang:1.22
    keep_alive_cmd: sleep infinity
    motd: "╔══════════════════════════════════════════════════════════════╗\n║   \
      \                Go 1.22 Quick Reference                     ║\n╚══════════════════════════════════════════════════════════════╝\n\
      \n\U0001F535 Go Environment:\n   go version                                \
      \    # Check Go version\n   go env                                        #\
      \ Show environment\n\n\U0001F4E6 Module Management:\n   go mod init myapp  \
      \                           # Initialize module\n   go mod tidy            \
      \                       # Clean dependencies\n   go get package@version    \
      \                    # Add dependency\n   go mod download                  \
      \             # Download dependencies\n\n\U0001F680 Build & Run:\n   go run\
      \ main.go                                # Run directly\n   go build -o myapp\
      \                             # Build executable\n   go build -o /shared/myapp\
      \                     # Build to shared\n\n\U0001F9EA Testing:\n   go test \
      \                                      # Run tests\n   go test -v          \
      \                          # Verbose output\n   go test -cover             \
      \                   # With coverage\n   go test -bench=.                   \
      \           # Run benchmarks\n\n\U0001F4DD Quick HTTP Server:\n   Create /shared/server.go:\n\
      \     package main\n     import (\"fmt\"; \"net/http\")\n     func main() {\n\
      \       http.HandleFunc(\"/\", func(w http.ResponseWriter, r *http.Request)\
      \ {\n         fmt.Fprintf(w, \"Hello from Go!\")\n       })\n       http.ListenAndServe(\"\
      :8080\", nil)\n     }\n   go run /shared/server.go\n\n\U0001F527 Useful Commands:\n\
      \   go fmt ./...                                  # Format code\n   go vet ./...\
      \                                  # Examine code\n   go doc package       \
      \                         # View documentation\n"
    shell: /bin/bash
