images:
  php-8.1:
    category: programming
    description: PHP 8.1
    image: php:8.1
    keep_alive_cmd: sleep infinity
    shell: /bin/bash
    motd: |
      ╔══════════════════════════════════════════════════════════════╗
      ║                  PHP 8.1 Development Environment              ║
      ╚══════════════════════════════════════════════════════════════╝

      🚀 Quick Commands:
         php -v                                    # Check PHP version
         php -i | head -20                         # Full PHP info
         php -m                                    # List loaded modules
         php -a                                    # Interactive shell (REPL)
         php -r "echo 'Hello World!';"             # Run PHP code directly

      📝 Quick Script Test:
         echo '<?php echo "PHP " . phpversion() . " is working!\n"; ?>' > /shared/test.php
         php /shared/test.php

      🔧 Development Server:
         php -S 0.0.0.0:8000 -t /shared
         # Access at http://localhost:8000

      🆕 PHP 8.1 Features:
         • Enumerations (Enums)
         • Readonly properties
         • Fibers (lightweight threads)
         • Array unpacking with string keys
         • Never return type

      📁 Useful Directories:
         /shared                                   # Shared with host
         /shared/projects                          # Your PHP projects

      💡 Tips:
         • PHP 8.1 LTS with enums support
         • Improved performance over 8.0
         • Keep projects in /shared for persistence

      📚 Documentation: https://www.php.net/releases/8.1/
