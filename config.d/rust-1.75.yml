images:
  rust-1.75:
    category: programming
    description: Rust 1.75 (Latest stable)
    image: rust:1.75
    keep_alive_cmd: sleep infinity
    motd: "╔══════════════════════════════════════════════════════════════╗\n║   \
      \               Rust 1.75 Quick Reference                    ║\n╚══════════════════════════════════════════════════════════════╝\n\
      \n\U0001F980 Rust Environment:\n   rustc --version                         \
      \      # Compiler version\n   cargo --version                              \
      \ # Cargo version\n   rustup update                                 # Update\
      \ Rust\n\n\U0001F4E6 Cargo Commands:\n   cargo new myapp                   \
      \            # Create new project\n   cargo init                           \
      \         # Initialize in current dir\n   cargo build                      \
      \             # Build debug\n   cargo build --release                      \
      \   # Build optimized\n   cargo run                                     # Build\
      \ and run\n   cargo test                                    # Run tests\n  \
      \ cargo check                                   # Check without building\n\n\
      \U0001F680 Quick Program:\n   Create /shared/hello.rs:\n     fn main() {\n \
      \        println!(\"Hello from Rust!\");\n         let numbers = vec![1, 2,\
      \ 3, 4, 5];\n         let sum: i32 = numbers.iter().sum();\n         println!(\"\
      Sum: {}\", sum);\n     }\n   rustc /shared/hello.rs -o /shared/hello\n   /shared/hello\n\
      \n\U0001F527 Useful Commands:\n   cargo fmt                                \
      \     # Format code\n   cargo clippy                                  # Lint\
      \ code\n   cargo doc --open                              # Generate docs\n \
      \  cargo clean                                   # Clean build\n\n\U0001F4CA\
      \ Dependencies:\n   cargo add serde                               # Add dependency\n\
      \   cargo update                                  # Update dependencies\n  \
      \ cargo tree                                    # Show dependency tree\n"
    shell: /bin/bash
